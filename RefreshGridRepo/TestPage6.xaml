<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="RefreshGridRepo.TestPage6"
             Title="TestPage6">

    <ScrollView>
        <VerticalStackLayout>
            <Border Padding="20">
                <Label Text="{Binding TestData}"></Label>
            </Border>
            
            <Button Text="Click me" Command="{Binding TestCommand}"></Button>
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>